block('image').mod('wrap', true)(
    def()(function() {
        this.ctx.title = this.ctx.caption;

        applyCtx({
            block : 'figure',
            caption : this.ctx.caption,
            content : this.ctx
        });
    })
);
